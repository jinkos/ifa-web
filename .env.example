## Core App
# Note: this must be .env, not .env.local, unless you change config.
POSTGRES_URL=postgresql://USER:PASSWORD@HOST:PORT/postgres
AUTH_SECRET=replace-with-random-64-byte-hex
BASE_URL=http://localhost:3000

## Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

## Supabase (server)
SUPABASE_URL=https://PROJECT_REF.supabase.co
SUPABASE_SERVICE_KEY=service_role_key_here

## Supabase (browser)
NEXT_PUBLIC_SUPABASE_URL=https://PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=anon_key_here

## Notes
# - DATABASE_URL is unused (POSTGRES_URL is used throughout)
# - SUPABASE_ANON_KEY (server var) is unused; use NEXT_PUBLIC_SUPABASE_ANON_KEY for browser code
# - Remove duplicate SUPABASE_SERVICE_KEY entries; keep only one
# - S3_* and STORAGE_ENDPOINT are unused unless you add a direct S3 client; Supabase Storage is used via the Supabase client

## Demo credentials (optional for Dev helpers)
# Used by the Dev page "log out and in" button
DEMO_LOGIN_EMAIL=
DEMO_LOGIN_PASSWORD=